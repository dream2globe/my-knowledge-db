# AGENTS.md — LLM Wiki 운영 규칙

이 파일은 Kilo가 `test-knowledge-db` 지식 베이스를 운영하는 방식을 정의한다.
Karpathy의 LLM Wiki 패턴을 기반으로 한다: LLM이 지식을 직접 컴파일하고 유지하는 "영속적, 복리적 아티팩트".

---

## 구조 규칙

```
markdown/
├── raw/        # 원본 소스 (불변 — 절대 수정하지 않는다)
├── wiki/       # LLM 관리 위키 (핵심 아티팩트)
│   ├── index.md   # 전체 목차 (항상 최신 상태 유지)
│   ├── log.md     # 작업 이력 (append-only)
│   └── pages/     # 위키 페이지들 (그래프 뷰 혼동 방지를 위해 분리)
│       └── *.md
└── scripts/    # 자동화 스크립트
```

- `raw/`는 절대 수정하지 않는다. 소스의 진실(source of truth).
- `wiki/`는 Kilo가 직접 생성·수정·삭제한다.
- 위키 페이지 파일명은 의미 있는 한국어 또는 영어 제목을 사용한다 (타임스탬프나 ID 금지).
  - 좋은 예: `비트코인 현물 ETF 분석.md`, `Transformer 아키텍처.md`
  - 나쁜 예: `1715000000_abc.md`, `page_001.md`

---

## 1. 자료 넣기 (Ingest)

새로운 raw/ 파일을 위키로 변환하는 작업.

### 트리거

사용자가 다음 중 하나를 요청할 때:
- `"raw/파일명.txt 를 ingest해줘"`
- `"새 자료 넣어줘"`
- `"위키 페이지 만들어줘"`

### 절차

1. **소스 파일 읽기**: `raw/` 디렉토리의 해당 파일을 읽는다.

2. **소스 타입 판별** (메타데이터 헤더 참조):
   - `URL: https://youtube.com` → YouTube transcript
   - `URL: (없음)` + `.pdf` 확장자 → PDF 논문
   - 그 외 → 웹 클립 / 기타

3. **카테고리 분류** (내용 기반):

   | 카테고리 | 키워드 / 채널 |
   |---|---|
   | 가상자산 | 비트코인, 이더리움, 알트코인, DeFi, NFT, 불장단타왕, 디파이 농부 조선생 |
   | 주식 | 나스닥, S&P500, 금리, ETF, 한경 글로벌마켓, 월가아재의 과학적 투자 |
   | AI·IT | LLM, GPT, 모델, 개발, 코딩, 개발동생, 조코딩, 실밸개발자 |
   | 논문 | Abstract, arXiv, 연구, 실험, 결과, NotebookLM |

4. **위키 페이지 생성**: `wiki/pages/{제목}.md` 형식으로 저장.
   - 제목은 소스 내용을 대표하는 한국어 또는 영어 구절 (5-15자 권장).
   - 아래 **위키 페이지 형식** 엄격히 준수.

5. **위키링크 연결**: 기존 위키 페이지와 관련 있는 항목을 `[[페이지 이름]]` 형식으로 연결한다.
   - 연결 전 `wiki/index.md`에서 기존 페이지 목록을 확인한다.

6. **index.md 업데이트**: 해당 카테고리 섹션에 새 항목 추가.
   - 형식: `- [[페이지 이름]] — 한 줄 요약`

7. **log.md 기록**: `## [YYYY-MM-DD] ingest | 제목` 형식으로 최상단에 추가.

---

## 2. 질문하기 (Query)

위키에 축적된 지식을 바탕으로 질문에 답하는 작업.

### 트리거

사용자가 지식 베이스 관련 질문을 할 때:
- `"비트코인에 대해 알고 있는 거 알려줘"`
- `"최근 AI 트렌드 정리해줘"`
- `"[주제]에 대해 위키에서 찾아봐"`

### 절차

1. `wiki/index.md`를 읽어 관련 카테고리와 페이지를 파악한다.
2. 관련성 높은 위키 페이지들을 읽는다 (최대 5-7개).
3. 교차 참조 (`[[위키링크]]`)를 따라 추가 컨텍스트를 수집한다.
4. 종합적인 답변을 생성한다.
5. 답변이 새로운 인사이트를 담고 있다면, 사용자 동의 후 새 위키 페이지로 저장할 수 있다.

---

## 3. 건강검진 (Lint)

위키의 품질과 일관성을 점검하는 작업.

### 트리거

사용자가 다음을 요청할 때:
- `"위키 건강검진 해줘"`
- `"위키 점검해줘"`
- `"lint 실행해줘"`

### 점검 항목

1. **Orphan 페이지**: `index.md`에 등재되지 않은 `wiki/*.md` 파일 탐지.
2. **끊어진 위키링크**: `[[링크]]` 형식 중 대상 파일이 없는 링크 탐지.
3. **중복 내용**: 동일한 주제를 다루는 페이지가 2개 이상인 경우 통합 제안.
4. **모순 내용**: 같은 사실에 대해 서로 다른 내용을 담은 페이지 탐지 및 플래그.
5. **최신성**: `log.md`에서 90일 이상 업데이트되지 않은 카테고리 확인.

### 보고 형식

```markdown
## 건강검진 결과 — YYYY-MM-DD

### ⚠️ 발견된 문제
- orphan: wiki/페이지명.md (index.md에 미등재)
- 끊어진 링크: wiki/A.md → [[존재하지 않는 페이지]]

### ✅ 정상
- 총 페이지: N개
- 총 위키링크: M개
- 마지막 ingest: YYYY-MM-DD
```

---

## 4. 위키 페이지 형식

모든 위키 페이지는 아래 형식을 **엄격히** 따른다.

```markdown
> [!info] 카테고리: {카테고리} | 소스: {YouTube/PDF/웹} | 날짜: {YYYY-MM-DD}
> 채널/출처: {채널명 또는 저자} | URL: {URL 또는 "없음"}

## 핵심 요약

{1-2문장. 이 페이지가 왜 중요한지, 무엇을 담고 있는지 블로그 포스트 첫 문단처럼 작성.}

---

## 상세 내용

### {섹션 1 제목}

{내용}

### {섹션 2 제목}

{내용}

---

## 관련 페이지

- [[관련 페이지 1]]
- [[관련 페이지 2]]
```

### 작성 원칙

- **충분한 설명**: 내용이 긴 원본 자료의 경우 너무 짧게 요약하지 말고, 독자가 맥락을 온전히 이해할 수 있도록 충분한 설명과 구체적인 내용을 포함하여 상세히 작성한다.
- **압축**: raw transcript의 반복, 잡담, 광고 등을 제거하되 핵심 정보와 맥락은 생략 없이 풍부하게 담는다.
- **구체성**: 숫자, 날짜, 고유명사는 원문 그대로 보존한다.
- **참고 링크 보존**: 원본 내용에 포함된 중요한 참조 링크(예: 논문, 공식 문서, 기타 참조 자료 등)는 페이지 하단이나 적절한 위치에 명시하여 추후 접근이 가능하도록 정리한다.
- **연결성**: 기존 위키 페이지와 최소 1개 이상 위키링크로 연결한다.
- **한국어 우선**: 한국어 소스는 한국어로, 영어 소스는 영어로 작성한다.

---

## 5. 소스 채널 분류 참고

| 카테고리 | 채널 / 출처 |
|---|---|
| 가상자산 | 불장단타왕, 디파이 농부 조선생 |
| 주식 투자 | 한경 글로벌마켓, 월가아재의 과학적 투자 |
| AI·IT | 개발동생, 조코딩, 실밸개발자, 노정석 |
| 논문 | PDF 파일, NotebookLM |

> **참고:** 노정석 YouTube 채널은 별도의 영상 추출 없이 `https://aifrontier.kr/ko/episodes/` 에서 내용을 바로 활용할 수 있습니다.

---

## 6. 스크립트 사용법

### YouTube 영상 ingest (전체 파이프라인)

```bash
cd /home/shyeon/workspace/project/test-knowledge-db
./markdown/scripts/ingest_youtube.sh "https://youtube.com/watch?v=VIDEO_ID" [--lang ko]
```

이 스크립트가 완료되면 `markdown/raw/{제목}.txt`가 생성된다.
그 후 Kilo에게 위키 페이지 생성을 요청한다.

### transcribe.py 직접 실행 (이미 다운로드된 오디오 파일)

```bash
.venv/bin/python markdown/scripts/transcribe.py audio.m4a \
    --output markdown/raw/제목.txt \
    --title "영상 제목" \
    --channel "채널명" \
    --url "https://..." \
    --language ko
```

---

## 7. 불변 규칙

1. `raw/` 파일은 절대 수정·삭제하지 않는다.
2. `wiki/log.md`는 append-only다. 기존 로그 항목을 수정하지 않는다.
3. `wiki/index.md`는 항상 실제 파일 목록과 일치해야 한다.
4. 위키 페이지 파일명에 타임스탬프나 랜덤 ID를 사용하지 않는다.
5. 사용자 동의 없이 위키 페이지를 삭제하지 않는다 (통합 제안만 한다).
